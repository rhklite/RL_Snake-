Analyze a completed training run and produce a structured diagnosis.

Read the skill at `~/.cursor/skills/rl-postmortem/SKILL.md` and follow its steps. Forward any arguments the user provides after `/postmortem` as the run directory path (e.g. `/postmortem runs/0315_14/`).

If no path is provided, auto-detect the most recent run directory under `runs/`.
